<UserControl x:Class="TTEngine.Editor.Panels.ScenePanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

        <Border Background="#2B2B2B" Padding="6">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="SCENES"
                           Foreground="White"
                           FontWeight="Bold"
                           VerticalAlignment="Center"/>

                    <Button Content="+"
                        Width="25"
                        Height="25"
                        Margin="8,0,0,0"
                        Click="NewScene_Click"/>
                </StackPanel>

                <!-- Scene List -->
            <ListBox x:Name="SceneList"
                     Grid.Row="1"
                     Background="#2B2B2B"
                     Foreground="White"
                     BorderThickness="0"
                     SelectionChanged="SceneList_SelectionChanged">

                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent"/>

                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter Property="Background" Value="#3C5A78"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>    
                </ListBox.ItemContainerStyle>
                
                <ListBox.ContextMenu>
                    <ContextMenu>
                        <MenuItem   Header="Rename"
                                    Click="Rename_Click"/>
                        
                        <MenuItem   Header="Delete"
                                    Click="Delete_Click"/>

                    </ContextMenu>    
                </ListBox.ContextMenu>
            </ListBox>
        </Grid>
        </Border>
    </UserControl>
